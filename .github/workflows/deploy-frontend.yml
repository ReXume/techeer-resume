# # 프론트엔드 배포 워크플로우
# # develop 브랜치에 프론트엔드 변경사항이 머지될 때 실행
# # 빌드 후 아티팩트 업로드 (실제 배포는 별도 설정 필요)
# # 라벨 기반이 아닌 자동 배포 (develop 브랜치 트리거)
# name: Deploy Frontend

# on:
#   push:
#     branches: [develop] # develop 브랜치에 푸시될 때만 실행
#     paths:
#       - "frontend/**" # 프론트엔드 디렉토리 변경 시에만 실행
#       - ".github/workflows/deploy-frontend.yml" # 워크플로우 파일 변경 시에도 실행

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       # 1. 소스코드 체크아웃
#       - name: Checkout code
#         uses: actions/checkout@v4

#       # 2. Node.js 18 환경 설정 및 Yarn 캐시
#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "18"
#           cache: "yarn" # Yarn 패키지 캐시 활성화
#           cache-dependency-path: frontend/yarn.lock

#       # 3. 의존성 설치 (lockfile 기반으로 정확한 버전 설치)
#       - name: Install dependencies
#         working-directory: ./frontend
#         run: yarn install --frozen-lockfile

#       # 4. 프로덕션 빌드 실행
#       - name: Build frontend
#         working-directory: ./frontend
#         run: yarn build

#       # 5. 빌드 결과물 아티팩트 업로드
#       - name: Upload build artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: frontend-build
#           path: frontend/dist/ # Vite 빌드 결과물

#       # 6. 배포 준비 완료 알림 (실제 배포는 별도 설정)
#       - name: Deploy to hosting service
#         run: |
#           echo "Frontend build completed successfully"
#           echo "Build artifacts uploaded for deployment"
#           # Add deployment steps here (e.g., S3, Vercel, Netlify, etc.)
