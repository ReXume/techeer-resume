# # 통합 테스트 워크플로우
# # Docker Compose를 사용한 전체 시스템 통합 테스트
# # 백엔드, 프론트엔드, 데이터베이스가 함께 동작하는지 검증
# # 라벨 기반 실행: ci:integration, ci:all, ci:full
# name: Integration Test

# on:
#   pull_request:
#     types: [opened, synchronize, labeled, unlabeled] # PR 생성, 업데이트, 라벨 변경 시 실행
#     branches: [main, develop] # main, develop 브랜치 대상

# jobs:
#   integration-test:
#     runs-on: ubuntu-latest
#     # 라벨 조건: ci:integration, ci:all, ci:full 중 하나가 있어야 실행
#     if: |
#       contains(github.event.pull_request.labels.*.name, 'ci:integration') ||
#       contains(github.event.pull_request.labels.*.name, 'ci:all') ||
#       contains(github.event.pull_request.labels.*.name, 'ci:full')

#     steps:
#       # 1. 소스코드 체크아웃
#       - name: Checkout code
#         uses: actions/checkout@v4

#       # 2. Docker Buildx 설정 (멀티 플랫폼 빌드 지원)
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       # 3. Docker Compose로 전체 서비스 시작
#       - name: Start services with Docker Compose
#         run: |
#           docker compose -f docker-compose.yml up -d --build

#       # 4. 서비스 준비 완료까지 대기 (최대 5분)
#       - name: Wait for services to be ready
#         run: |
#           timeout 300 bash -c 'until curl -f http://localhost:8080/actuator/health; do sleep 5; done'

#       # 5. 통합 테스트 실행 (API 테스트, E2E 테스트 등)
#       - name: Run integration tests
#         run: |
#           # Add integration test commands here
#           echo "Running integration tests..."
#           # Example: curl tests, API tests, etc.

#       # 6. 테스트 완료 후 서비스 정리 (실패해도 정리)
#       - name: Stop services
#         if: always() # 테스트 실패해도 컨테이너 정리
#         run: |
#           docker compose -f docker-compose.yml down -v
