# # 백엔드 배포 워크플로우
# # develop 브랜치에 백엔드 변경사항이 머지될 때 실행
# # Docker 이미지 빌드 후 Docker Hub에 푸시
# # 라벨 기반이 아닌 자동 배포 (develop 브랜치 트리거)
# name: Deploy Backend

# on:
#   push:
#     branches: [develop] # develop 브랜치에 푸시될 때만 실행
#     paths:
#       - "backend/**" # 백엔드 디렉토리 변경 시에만 실행
#       - ".github/workflows/deploy-backend.yml" # 워크플로우 파일 변경 시에도 실행

# # Docker Hub 설정
# env:
#   REGISTRY: docker.io
#   IMAGE_NAME: techeer-resume-backend

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     # Docker Hub 푸시를 위한 권한 설정
#     permissions:
#       contents: read # 소스코드 읽기 권한
#       packages: write # 패키지(이미지) 쓰기 권한

#     steps:
#       # 1. 소스코드 체크아웃
#       - name: Checkout code
#         uses: actions/checkout@v4

#       # 2. Docker Buildx 설정 (멀티 플랫폼 빌드 지원)
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       # 3. Docker Hub 로그인 (시크릿 사용)
#       - name: Log in to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       # 4. Docker 이미지 메타데이터 생성 (태그, 라벨 등)
#       - name: Extract metadata
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
#           tags: |
#             type=ref,event=branch  # 브랜치명으로 태그 생성
#             type=sha,prefix={{branch}}-  # 커밋 SHA로 태그 생성
#             type=raw,value=latest,enable=${{ github.ref == 'refs/heads/develop' }}  # develop 브랜치 푸시에 latest 태그

#       # 5. Docker 이미지 빌드 및 푸시 (캐시 활용)
#       - name: Build and push Docker image
#         uses: docker/build-push-action@v5
#         with:
#           context: ./backend # 백엔드 디렉토리를 빌드 컨텍스트로 사용
#           file: ./backend/Dockerfile
#           push: true # Docker Hub에 푸시
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha # GitHub Actions 캐시에서 빌드 캐시 로드
#           cache-to: type=gha,mode=max # GitHub Actions 캐시에 빌드 캐시 저장

#       # 6. 배포 성공 알림
#       - name: Notify deployment success
#         run: |
#           echo "Backend Docker image has been successfully built and pushed to Docker Hub"
#           echo "Image tags: ${{ steps.meta.outputs.tags }}"
